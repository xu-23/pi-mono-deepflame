# question

Structured question tool for pi.

## Install

```bash
mkdir -p ~/.pi/agent/extensions
cp -R extra-extensions/extensions/question ~/.pi/agent/extensions/question
```

Restart `pi` or run `/reload` after copying the plugin folder.
